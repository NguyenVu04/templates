"""FastAPI service.

- ``main``         application object and routes
- ``schemas``      request/response models — the public contract
- ``inference``    artifact loading and prediction
- ``dependencies`` shared, cached resources injected into routes
"""
